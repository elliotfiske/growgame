using UnityEngine;
using System.Collections;

public class Res : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Screen.SetResolution (400, 700, false);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
